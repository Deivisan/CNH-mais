# Imagens de Perfil (Placeholder)

As imagens usam fallback SVG inline via atributo `onerror` no HTML.

## Fallbacks implementados:
- candidato-joao.jpg → SVG com iniciais "JS" (azul #4A90D9)
- instrutor-maria.jpg → SVG com iniciais "MS" (azul #4A90D9)

Caso as imagens não carreguem, os placeholders SVG são exibidos automaticamente.
